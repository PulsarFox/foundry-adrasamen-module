/**
 * Hook to handle post-advancement cleanup (optional)
 */

export function initAdvancementHooks() {
	// Currently no hooks needed - the dynamic calculation handles everything
	console.log("Adrasamen | Advancement hooks initialized (no active hooks)");
}
